# Twilio Configuration (for SMS)
TWILIO_SID=your_twilio_account_sid
TWILIO_TOKEN=your_twilio_auth_token
TWILIO_NUMBER=+1234567890

# Recipient phone numbers (comma-separated, E.164 format)
RECIPIENT_NUMBERS=+1234567890,+0987654321

# Park entity IDs to monitor (comma-separated)
# Find IDs at https://api.themeparks.wiki/v1/destinations
# Example: Magic Kingdom = 75ea578a-adc8-4116-a54d-dccb60765ef9
PARK_IDS=75ea578a-adc8-4116-a54d-dccb60765ef9

# Optional: Specific rides to watch (comma-separated, partial match)
# Leave empty to monitor all attractions
WATCHED_RIDES=Space Mountain,Haunted Mansion,Pirates

# Google Cloud project ID (set automatically in Cloud Run)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Port (Cloud Run sets this automatically)
PORT=8080

# Dynamic Scheduling (Cloud Tasks)
# Set to 'true' to enable dynamic scheduling based on ride status
DYNAMIC_SCHEDULING=true

# Cloud Tasks configuration
CLOUD_TASKS_LOCATION=us-central1
CLOUD_TASKS_QUEUE=ride-watch-queue

# Service URL (your Cloud Run service URL, required for dynamic scheduling)
SERVICE_URL=https://ride-watch-xxxxx-uc.a.run.app

# Check intervals (seconds)
NORMAL_INTERVAL_SEC=300  # 5 minutes when all rides operating
ALERT_INTERVAL_SEC=60    # 1 minute when any ride is down

# Notification channels (both enabled by default)
ENABLE_SMS=true   # Set to 'false' to disable SMS notifications
ENABLE_PUSH=true  # Set to 'false' to disable push notifications
